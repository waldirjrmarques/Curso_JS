/* 
    Operadores Ternario
        teste ? true : false
        Ex1:
*/
            let media = 7
                let res = media >= 7 ? `Aprovado` : `Reprovado` 
                    console.log(res)  

/*
        Ex2:
*/ console.log(`-----------------------------`)
            
            let idade = 17
                let resp = idade >= 18 ? `Maior de Idade`:`Menor de idade`
                console.log(resp)  

/*
        Ex3:
*/ console.log(`-----------------------------`)  
        
            