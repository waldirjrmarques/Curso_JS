function parimp(n) {
    if (n % 2 == 0) {
        return `Par`
    }else{
        return `Impar`
    }  
}

let res = parimp(11)
console.log(res)

/* OU
    console.log(parimp(11))
*/

// Uma função em J.S só pode ter um retonto. 